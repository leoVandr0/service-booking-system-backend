# Production configuration for Railway
spring.profiles.active=prod

# PostgreSQL Database configuration for Railway
spring.datasource.url=jdbc:postgresql://containers-us-west-163.railway.app:5432/railway
spring.datasource.username=postgres
spring.datasource.password=quFsjUmshlfpxKRarwcevUSRwdsyKJCG
spring.datasource.driver-class-name=org.postgresql.Driver

jakarta.persistence.jdbc.url=${spring.datasource.url}


# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Production logging
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.org.hibernate=WARN
server.error.include-stacktrace=never

# Security headers for production
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

# Health check endpoint for Railway
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
